# Database
DB_HOST=localhost
DB_NAME=movie_db
DB_USER=root
DB_PASS=yourpassword

# JWT
JWT_SECRET=change-me

# Service URLs for gateway (if using remote services)
MOVIE_SERVICE_URL=http://localhost:4001
USER_SERVICE_URL=http://localhost:4002
